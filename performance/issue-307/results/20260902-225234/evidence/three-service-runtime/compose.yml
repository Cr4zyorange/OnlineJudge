# Generated by scripts/platform/render_disposable_environment.py; do not edit.
# Source of truth: deploy/platform/workloads.json
name: ${COMPOSE_PROJECT_NAME:-onlinejudge-platform}
services:
  gateway:
    image: "onlinejudge/gateway:c66686ff0e011f5ee63e3908683f01afd4f83ebc"
    build:
      context: "/private/tmp/oj307-three-c666"
      dockerfile: "services/gateway/Dockerfile"
      args:
        GIT_SHA: "c66686ff0e011f5ee63e3908683f01afd4f83ebc"
    environment:
      ASSESSMENT_UPSTREAM: "assessment-api:8083"
      COURSE_UPSTREAM: "course-service:8082"
      GATEWAY_SERVICE_IDENTITY: "${GATEWAY_SERVICE_IDENTITY:?GATEWAY_SERVICE_IDENTITY is required}"
      GIT_SHA: "c66686ff0e011f5ee63e3908683f01afd4f83ebc"
      GRADE_UPSTREAM: "grade-service:8084"
      IDENTITY_JWKS_TRUST_BUNDLE: "${IDENTITY_JWKS_TRUST_BUNDLE:-}"
      IDENTITY_JWT_AUDIENCE: "onlinejudge.api"
      IDENTITY_JWT_ISSUER: "onlinejudge.identity.v2"
      IDENTITY_UPSTREAM: "identity-service:8081"
      ISSUE318_FAIL_READINESS: "${ISSUE318_FAIL_READINESS:-0}"
    depends_on:
      identity-service:
        condition: service_healthy
      course-service:
        condition: service_healthy
      assessment-api:
        condition: service_healthy
      grade-service:
        condition: service_healthy
      frontend:
        condition: service_healthy
    ports:
      - "${GATEWAY_HTTP_PORT:-18080}:8080"
    healthcheck:
      test: ["CMD-SHELL", "test \"$${ISSUE318_FAIL_READINESS:-0}\" != \"1\" && wget -qO- http://127.0.0.1:8080/health/ready | grep -q '\"status\":\"UP\"'"]
      interval: 5s
      timeout: 3s
      retries: 24
      start_period: 10s
    deploy:
      resources:
        reservations:
          cpus: "0.1"
          memory: "128M"
        limits:
          cpus: "0.5"
          memory: "512M"
    restart: "no"
  identity-service:
    image: "onlinejudge/identity-service:c66686ff0e011f5ee63e3908683f01afd4f83ebc"
    build:
      context: "/private/tmp/oj307-three-c666"
      dockerfile: "services/identity/Dockerfile"
      args:
        GIT_SHA: "c66686ff0e011f5ee63e3908683f01afd4f83ebc"
    environment:
      GIT_SHA: "c66686ff0e011f5ee63e3908683f01afd4f83ebc"
      IDENTITY_DATABASE_HOST: "mysql"
      IDENTITY_DATABASE_NAME: "oj_identity"
      IDENTITY_DATABASE_PASSWORD: "${IDENTITY_DATABASE_PASSWORD:?IDENTITY_DATABASE_PASSWORD is required}"
      IDENTITY_DATABASE_PORT: "3306"
      IDENTITY_DATABASE_USERNAME: "oj_identity_rw"
      IDENTITY_JWT_AUDIENCE: "onlinejudge.api"
      IDENTITY_JWT_ISSUER: "onlinejudge.identity.v2"
      IDENTITY_JWT_KID: "issue318-disposable"
      IDENTITY_JWT_SIGNING_KEY: "${IDENTITY_JWT_SIGNING_KEY:?IDENTITY_JWT_SIGNING_KEY is required}"
      IDENTITY_SEED_DATA_ENABLED: "false"
      IDENTITY_SERVICE_REVISION: "c66686ff0e011f5ee63e3908683f01afd4f83ebc"
      ISSUE318_FAIL_READINESS: "${ISSUE318_FAIL_READINESS:-0}"
    depends_on:
      mysql:
        condition: service_healthy
      identity-migrations:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD-SHELL", "test \"$${ISSUE318_FAIL_READINESS:-0}\" != \"1\" && wget -qO- http://127.0.0.1:8081/api/v1/system/readiness | grep -q '\"status\":\"UP\"'"]
      interval: 5s
      timeout: 3s
      retries: 24
      start_period: 10s
    deploy:
      resources:
        reservations:
          cpus: "0.2"
          memory: "256M"
        limits:
          cpus: "1"
          memory: "1G"
    restart: "no"
  course-service:
    image: "onlinejudge/course-service:c66686ff0e011f5ee63e3908683f01afd4f83ebc"
    build:
      context: "/private/tmp/oj307-three-c666"
      dockerfile: "services/course/Dockerfile"
      args:
        GIT_SHA: "c66686ff0e011f5ee63e3908683f01afd4f83ebc"
    environment:
      COURSE_DATABASE_HOST: "mysql"
      COURSE_DATABASE_NAME: "oj_course"
      COURSE_DATABASE_PASSWORD: "${COURSE_DATABASE_PASSWORD:?COURSE_DATABASE_PASSWORD is required}"
      COURSE_DATABASE_PORT: "3306"
      COURSE_DATABASE_USER: "oj_course_rw"
      COURSE_RABBIT_ENABLED: "true"
      COURSE_SERVICE_IDENTITY: "${COURSE_SERVICE_IDENTITY:?COURSE_SERVICE_IDENTITY is required}"
      GIT_SHA: "c66686ff0e011f5ee63e3908683f01afd4f83ebc"
      IDENTITY_JWKS_TRUST_BUNDLE: "${IDENTITY_JWKS_TRUST_BUNDLE:-}"
      IDENTITY_JWKS_URI: "http://identity-service:8081/.well-known/jwks.json"
      IDENTITY_JWT_AUDIENCE: "onlinejudge.api"
      IDENTITY_JWT_ISSUER: "onlinejudge.identity.v2"
      ISSUE318_FAIL_READINESS: "${ISSUE318_FAIL_READINESS:-0}"
      RABBITMQ_HOST: "rabbitmq"
      RABBITMQ_PASSWORD: "${RABBITMQ_PASSWORD:?RABBITMQ_PASSWORD is required}"
      RABBITMQ_PORT: "5672"
      RABBITMQ_USER: "onlinejudge"
    depends_on:
      mysql:
        condition: service_healthy
      course-migrations:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD-SHELL", "test \"$${ISSUE318_FAIL_READINESS:-0}\" != \"1\" && wget -qO- http://127.0.0.1:8082/actuator/health/readiness | grep -q '\"status\":\"UP\"'"]
      interval: 5s
      timeout: 3s
      retries: 24
      start_period: 10s
    deploy:
      resources:
        reservations:
          cpus: "0.2"
          memory: "256M"
        limits:
          cpus: "1"
          memory: "1G"
    restart: "no"
  assessment-api:
    image: "onlinejudge/assessment-api:c66686ff0e011f5ee63e3908683f01afd4f83ebc"
    build:
      context: "/private/tmp/oj307-three-c666"
      dockerfile: "services/assessment/Dockerfile"
      args:
        GIT_SHA: "c66686ff0e011f5ee63e3908683f01afd4f83ebc"
    environment:
      ASSESSMENT_COURSE_AUTHORIZATION_URI: "http://course-service:8082/internal/v2/courses/{courseId}/members/{userId}/authorization"
      ASSESSMENT_COURSE_SERVICE_AUTHORIZATION: "${ASSESSMENT_SERVICE_IDENTITY:?ASSESSMENT_SERVICE_IDENTITY is required}"
      ASSESSMENT_DATABASE_HOST: "mysql"
      ASSESSMENT_DATABASE_NAME: "oj_assessment"
      ASSESSMENT_DATABASE_PASSWORD: "${ASSESSMENT_DATABASE_PASSWORD:?ASSESSMENT_DATABASE_PASSWORD is required}"
      ASSESSMENT_DATABASE_PORT: "3306"
      ASSESSMENT_DATABASE_USER: "oj_assessment_rw"
      ASSESSMENT_RABBIT_ENABLED: "false"
      ASSESSMENT_RABBIT_HOST: "rabbitmq"
      ASSESSMENT_RABBIT_RELAY_ENABLED: "false"
      ASSESSMENT_RABBIT_USERNAME: "onlinejudge"
      ASSESSMENT_SERVICE_IDENTITY: "${ASSESSMENT_SERVICE_IDENTITY:?ASSESSMENT_SERVICE_IDENTITY is required}"
      ASSESSMENT_STORAGE_ROOT: "/var/lib/onlinejudge-assessment"
      GIT_SHA: "c66686ff0e011f5ee63e3908683f01afd4f83ebc"
      IDENTITY_JWKS_TRUST_BUNDLE: "${IDENTITY_JWKS_TRUST_BUNDLE:-}"
      IDENTITY_JWKS_URI: "http://identity-service:8081/.well-known/jwks.json"
      IDENTITY_JWT_AUDIENCE: "onlinejudge.api"
      IDENTITY_JWT_ISSUER: "onlinejudge.identity.v2"
      ISSUE318_FAIL_READINESS: "${ISSUE318_FAIL_READINESS:-0}"
      RABBITMQ_PASSWORD: "${RABBITMQ_PASSWORD:?RABBITMQ_PASSWORD is required}"
    depends_on:
      mysql:
        condition: service_healthy
      assessment-migrations:
        condition: service_completed_successfully
    volumes:
      - assessment-files:/var/lib/onlinejudge-assessment
    healthcheck:
      test: ["CMD-SHELL", "test \"$${ISSUE318_FAIL_READINESS:-0}\" != \"1\" && wget -qO- http://127.0.0.1:8083/health/ready | grep -q '\"status\":\"UP\"'"]
      interval: 5s
      timeout: 3s
      retries: 24
      start_period: 10s
    deploy:
      resources:
        reservations:
          cpus: "0.3"
          memory: "512M"
        limits:
          cpus: "1.5"
          memory: "2G"
    restart: "no"
  assessment-worker:
    image: "onlinejudge/assessment-worker:c66686ff0e011f5ee63e3908683f01afd4f83ebc"
    build:
      context: "/private/tmp/oj307-three-c666"
      dockerfile: "services/assessment/Dockerfile"
      args:
        GIT_SHA: "c66686ff0e011f5ee63e3908683f01afd4f83ebc"
    environment:
      ASSESSMENT_COURSE_AUTHORIZATION_URI: "http://course-service:8082/internal/v2/courses/{courseId}/members/{userId}/authorization"
      ASSESSMENT_COURSE_SERVICE_AUTHORIZATION: "${ASSESSMENT_SERVICE_IDENTITY:?ASSESSMENT_SERVICE_IDENTITY is required}"
      ASSESSMENT_DATABASE_HOST: "mysql"
      ASSESSMENT_DATABASE_NAME: "oj_assessment"
      ASSESSMENT_DATABASE_PASSWORD: "${ASSESSMENT_DATABASE_PASSWORD:?ASSESSMENT_DATABASE_PASSWORD is required}"
      ASSESSMENT_DATABASE_PORT: "3306"
      ASSESSMENT_DATABASE_USER: "oj_assessment_rw"
      ASSESSMENT_RABBIT_ENABLED: "true"
      ASSESSMENT_RABBIT_HOST: "rabbitmq"
      ASSESSMENT_RABBIT_RELAY_ENABLED: "true"
      ASSESSMENT_RABBIT_USERNAME: "onlinejudge"
      ASSESSMENT_SANDBOX_DOCKER_API_URI: "${ASSESSMENT_SANDBOX_DOCKER_API_URI:-}"
      ASSESSMENT_STORAGE_ROOT: "/var/lib/onlinejudge-assessment"
      ASSESSMENT_WORKER_ENABLED: "true"
      ASSESSMENT_WORKER_IDENTITY: "${ASSESSMENT_WORKER_IDENTITY:?ASSESSMENT_WORKER_IDENTITY is required}"
      GIT_SHA: "c66686ff0e011f5ee63e3908683f01afd4f83ebc"
      IDENTITY_JWKS_URI: "http://identity-service:8081/.well-known/jwks.json"
      IDENTITY_JWT_AUDIENCE: "onlinejudge.api"
      IDENTITY_JWT_ISSUER: "onlinejudge.identity.v2"
      ISSUE318_FAIL_READINESS: "${ISSUE318_FAIL_READINESS:-0}"
      RABBITMQ_PASSWORD: "${RABBITMQ_PASSWORD:?RABBITMQ_PASSWORD is required}"
    depends_on:
      mysql:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      assessment-migrations:
        condition: service_completed_successfully
    volumes:
      - assessment-files:/var/lib/onlinejudge-assessment
    command: ["--spring.main.web-application-type=none", "--assessment.worker.enabled=true"]
    healthcheck:
      test: ["CMD-SHELL", "test \"$${ISSUE318_FAIL_READINESS:-0}\" != \"1\" && test -f /tmp/assessment-worker-ready"]
      interval: 5s
      timeout: 3s
      retries: 24
      start_period: 10s
    deploy:
      resources:
        reservations:
          cpus: "0.5"
          memory: "1G"
        limits:
          cpus: "2"
          memory: "4G"
    restart: "no"
  grade-service:
    image: "onlinejudge/grade-service:c66686ff0e011f5ee63e3908683f01afd4f83ebc"
    build:
      context: "/private/tmp/oj307-three-c666"
      dockerfile: "services/grade/Dockerfile"
      args:
        GIT_SHA: "c66686ff0e011f5ee63e3908683f01afd4f83ebc"
    environment:
      GIT_SHA: "c66686ff0e011f5ee63e3908683f01afd4f83ebc"
      GRADE_ASSESSMENT_BASE_URL: "http://assessment-api:8083"
      GRADE_ASSESSMENT_SERVICE_AUTHORIZATION: "${GRADE_SERVICE_IDENTITY:?GRADE_SERVICE_IDENTITY is required}"
      GRADE_COURSE_BASE_URL: "http://course-service:8082"
      GRADE_COURSE_SERVICE_AUTHORIZATION: "${GRADE_SERVICE_IDENTITY:?GRADE_SERVICE_IDENTITY is required}"
      GRADE_DATABASE_DRIVER: "com.mysql.cj.jdbc.Driver"
      GRADE_DATABASE_PASSWORD: "${GRADE_DATABASE_PASSWORD:?GRADE_DATABASE_PASSWORD is required}"
      GRADE_DATABASE_USER: "oj_grade_rw"
      GRADE_DATASOURCE_URL: "jdbc:mysql://mysql:3306/oj_grade?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC&allowPublicKeyRetrieval=true&useSSL=false"
      GRADE_HTTP_PORT: "8084"
      GRADE_RABBIT_ENABLED: "true"
      GRADE_RABBIT_RELAY_ENABLED: "true"
      GRADE_SERVICE_IDENTITY: "${GRADE_SERVICE_IDENTITY:?GRADE_SERVICE_IDENTITY is required}"
      IDENTITY_JWKS_TRUST_BUNDLE: "${IDENTITY_JWKS_TRUST_BUNDLE:-}"
      IDENTITY_JWKS_URI: "http://identity-service:8081/.well-known/jwks.json"
      IDENTITY_JWT_AUDIENCE: "onlinejudge.api"
      IDENTITY_JWT_ISSUER: "onlinejudge.identity.v2"
      ISSUE318_FAIL_READINESS: "${ISSUE318_FAIL_READINESS:-0}"
      RABBITMQ_HOST: "rabbitmq"
      RABBITMQ_PASSWORD: "${RABBITMQ_PASSWORD:?RABBITMQ_PASSWORD is required}"
      RABBITMQ_USERNAME: "onlinejudge"
    depends_on:
      mysql:
        condition: service_healthy
      grade-migrations:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD-SHELL", "test \"$${ISSUE318_FAIL_READINESS:-0}\" != \"1\" && wget -qO- http://127.0.0.1:8084/health/ready | grep -q '\"status\":\"UP\"'"]
      interval: 5s
      timeout: 3s
      retries: 24
      start_period: 10s
    deploy:
      resources:
        reservations:
          cpus: "0.2"
          memory: "256M"
        limits:
          cpus: "1"
          memory: "1G"
    restart: "no"
  frontend:
    image: "onlinejudge/frontend:c66686ff0e011f5ee63e3908683f01afd4f83ebc"
    build:
      context: "/private/tmp/oj307-three-c666"
      dockerfile: "deploy/docker/frontend.Dockerfile"
      args:
        GIT_SHA: "c66686ff0e011f5ee63e3908683f01afd4f83ebc"
    environment:
      GIT_SHA: "c66686ff0e011f5ee63e3908683f01afd4f83ebc"
      IDENTITY_JWT_AUDIENCE: "onlinejudge.api"
      IDENTITY_JWT_ISSUER: "onlinejudge.identity.v2"
      ISSUE318_FAIL_READINESS: "${ISSUE318_FAIL_READINESS:-0}"
    volumes:
      - "/private/tmp/oj307-perf.Vwbe8B/performance/issue-307/results/20260902-225234/evidence/three-service-runtime/frontend-disposable.conf:/etc/nginx/conf.d/default.conf:ro"
    healthcheck:
      test: ["CMD-SHELL", "test \"$${ISSUE318_FAIL_READINESS:-0}\" != \"1\" && wget -qO- http://127.0.0.1:80/ >/dev/null"]
      interval: 5s
      timeout: 3s
      retries: 24
      start_period: 10s
    deploy:
      resources:
        reservations:
          cpus: "0.1"
          memory: "128M"
        limits:
          cpus: "0.5"
          memory: "512M"
    restart: "no"
  rabbitmq:
    image: "rabbitmq:4.1-management"
    environment:
      RABBITMQ_DEFAULT_USER: "onlinejudge"
      RABBITMQ_DEFAULT_PASS: "${RABBITMQ_PASSWORD:?RABBITMQ_PASSWORD is required}"
    healthcheck:
      test: ["CMD-SHELL", "rabbitmq-diagnostics -q check_running"]
      interval: 5s
      timeout: 3s
      retries: 24
      start_period: 10s
    deploy:
      resources:
        reservations:
          cpus: "0.2"
          memory: "256M"
        limits:
          cpus: "1"
          memory: "1G"
    restart: "no"
  mysql:
    image: "mysql:8.4"
    environment:
      MYSQL_ROOT_PASSWORD: "${MYSQL_ROOT_PASSWORD:?MYSQL_ROOT_PASSWORD is required}"
    command: ["--character-set-server=utf8mb4", "--collation-server=utf8mb4_unicode_ci"]
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h 127.0.0.1 -uroot -p$$MYSQL_ROOT_PASSWORD --silent"]
      interval: 5s
      timeout: 3s
      retries: 24
      start_period: 10s
    deploy:
      resources:
        reservations:
          cpus: "0.5"
          memory: "1G"
        limits:
          cpus: "2"
          memory: "4G"
    restart: "no"
  mysql-runtime-account-init:
    image: "mysql:8.4"
    depends_on:
      mysql:
        condition: service_healthy
    environment:
      MYSQL_ROOT_PASSWORD: "${MYSQL_ROOT_PASSWORD:?MYSQL_ROOT_PASSWORD is required}"
      IDENTITY_DATABASE_PASSWORD: "${IDENTITY_DATABASE_PASSWORD:?IDENTITY_DATABASE_PASSWORD is required}"
      COURSE_DATABASE_PASSWORD: "${COURSE_DATABASE_PASSWORD:?COURSE_DATABASE_PASSWORD is required}"
      ASSESSMENT_DATABASE_PASSWORD: "${ASSESSMENT_DATABASE_PASSWORD:?ASSESSMENT_DATABASE_PASSWORD is required}"
      GRADE_DATABASE_PASSWORD: "${GRADE_DATABASE_PASSWORD:?GRADE_DATABASE_PASSWORD is required}"
    entrypoint: ["sh", "-ec", "export MYSQL_PWD=\"$$MYSQL_ROOT_PASSWORD\"\nescaped_password=$$(printf '%s' \"$${IDENTITY_DATABASE_PASSWORD}\" | sed -e 's/\\\\/\\\\\\\\/g' -e \"s/'/''/g\")\nmysql --protocol=tcp --host=mysql --port=3306 --user=root --execute \"CREATE DATABASE IF NOT EXISTS oj_identity CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci; CREATE USER IF NOT EXISTS 'oj_identity_rw'@'%' IDENTIFIED BY '$$escaped_password'; ALTER USER 'oj_identity_rw'@'%' IDENTIFIED BY '$$escaped_password'; REVOKE ALL PRIVILEGES, GRANT OPTION FROM 'oj_identity_rw'@'%'; GRANT SELECT, INSERT, UPDATE, DELETE ON oj_identity.* TO 'oj_identity_rw'@'%';\"\nescaped_password=$$(printf '%s' \"$${COURSE_DATABASE_PASSWORD}\" | sed -e 's/\\\\/\\\\\\\\/g' -e \"s/'/''/g\")\nmysql --protocol=tcp --host=mysql --port=3306 --user=root --execute \"CREATE DATABASE IF NOT EXISTS oj_course CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci; CREATE USER IF NOT EXISTS 'oj_course_rw'@'%' IDENTIFIED BY '$$escaped_password'; ALTER USER 'oj_course_rw'@'%' IDENTIFIED BY '$$escaped_password'; REVOKE ALL PRIVILEGES, GRANT OPTION FROM 'oj_course_rw'@'%'; GRANT SELECT, INSERT, UPDATE, DELETE ON oj_course.* TO 'oj_course_rw'@'%';\"\nescaped_password=$$(printf '%s' \"$${ASSESSMENT_DATABASE_PASSWORD}\" | sed -e 's/\\\\/\\\\\\\\/g' -e \"s/'/''/g\")\nmysql --protocol=tcp --host=mysql --port=3306 --user=root --execute \"CREATE DATABASE IF NOT EXISTS oj_assessment CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci; CREATE USER IF NOT EXISTS 'oj_assessment_rw'@'%' IDENTIFIED BY '$$escaped_password'; ALTER USER 'oj_assessment_rw'@'%' IDENTIFIED BY '$$escaped_password'; REVOKE ALL PRIVILEGES, GRANT OPTION FROM 'oj_assessment_rw'@'%'; GRANT SELECT, INSERT, UPDATE, DELETE ON oj_assessment.* TO 'oj_assessment_rw'@'%';\"\nescaped_password=$$(printf '%s' \"$${GRADE_DATABASE_PASSWORD}\" | sed -e 's/\\\\/\\\\\\\\/g' -e \"s/'/''/g\")\nmysql --protocol=tcp --host=mysql --port=3306 --user=root --execute \"CREATE DATABASE IF NOT EXISTS oj_grade CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci; CREATE USER IF NOT EXISTS 'oj_grade_rw'@'%' IDENTIFIED BY '$$escaped_password'; ALTER USER 'oj_grade_rw'@'%' IDENTIFIED BY '$$escaped_password'; REVOKE ALL PRIVILEGES, GRANT OPTION FROM 'oj_grade_rw'@'%'; GRANT SELECT, INSERT, UPDATE, DELETE ON oj_grade.* TO 'oj_grade_rw'@'%';\"\nmysql --protocol=tcp --host=mysql --port=3306 --user=root --execute \"FLUSH PRIVILEGES;\""]
    restart: "no"
  identity-migrations:
    image: "onlinejudge/platform-migration-runner:c66686ff0e011f5ee63e3908683f01afd4f83ebc"
    build:
      context: "/private/tmp/oj307-three-c666"
      dockerfile: "deploy/platform/migration-runner.Dockerfile"
      args:
        GIT_SHA: "c66686ff0e011f5ee63e3908683f01afd4f83ebc"
    depends_on:
      mysql-runtime-account-init:
        condition: service_completed_successfully
    environment:
      MYSQL_HOST: "mysql"
      MYSQL_PORT: "3306"
      MIGRATION_DATABASE_NAME: "oj_identity"
      MIGRATION_DATABASE_USER: "root"
      MIGRATION_DATABASE_PASSWORD: "${MYSQL_ROOT_PASSWORD:?MYSQL_ROOT_PASSWORD is required}"
      MIGRATION_ROOT: "/workspace/migrations"
      ISSUE318_FAIL_MIGRATION: "${ISSUE318_FAIL_MIGRATION:-0}"
    entrypoint: ["sh", "-ec", "if [ \"$${ISSUE318_FAIL_MIGRATION:-0}\" = \"1\" ]; then echo \"controlled migration failure: identity-migrations\" >&2; exit 41; fi; exec /workspace/migrate-service.sh --schema identity"]
    restart: "no"
  course-migrations:
    image: "onlinejudge/platform-migration-runner:c66686ff0e011f5ee63e3908683f01afd4f83ebc"
    build:
      context: "/private/tmp/oj307-three-c666"
      dockerfile: "deploy/platform/migration-runner.Dockerfile"
      args:
        GIT_SHA: "c66686ff0e011f5ee63e3908683f01afd4f83ebc"
    depends_on:
      mysql-runtime-account-init:
        condition: service_completed_successfully
      identity-migrations:
        condition: service_completed_successfully
    environment:
      MYSQL_HOST: "mysql"
      MYSQL_PORT: "3306"
      MIGRATION_DATABASE_NAME: "oj_course"
      MIGRATION_DATABASE_USER: "root"
      MIGRATION_DATABASE_PASSWORD: "${MYSQL_ROOT_PASSWORD:?MYSQL_ROOT_PASSWORD is required}"
      MIGRATION_ROOT: "/workspace/migrations"
      ISSUE318_FAIL_MIGRATION: "${ISSUE318_FAIL_MIGRATION:-0}"
    entrypoint: ["sh", "-ec", "if [ \"$${ISSUE318_FAIL_MIGRATION:-0}\" = \"1\" ]; then echo \"controlled migration failure: course-migrations\" >&2; exit 41; fi; exec /workspace/migrate-service.sh --schema course"]
    restart: "no"
  assessment-migrations:
    image: "onlinejudge/platform-migration-runner:c66686ff0e011f5ee63e3908683f01afd4f83ebc"
    build:
      context: "/private/tmp/oj307-three-c666"
      dockerfile: "deploy/platform/migration-runner.Dockerfile"
      args:
        GIT_SHA: "c66686ff0e011f5ee63e3908683f01afd4f83ebc"
    depends_on:
      mysql-runtime-account-init:
        condition: service_completed_successfully
      course-migrations:
        condition: service_completed_successfully
    environment:
      MYSQL_HOST: "mysql"
      MYSQL_PORT: "3306"
      MIGRATION_DATABASE_NAME: "oj_assessment"
      MIGRATION_DATABASE_USER: "root"
      MIGRATION_DATABASE_PASSWORD: "${MYSQL_ROOT_PASSWORD:?MYSQL_ROOT_PASSWORD is required}"
      MIGRATION_ROOT: "/workspace/migrations"
      ISSUE318_FAIL_MIGRATION: "${ISSUE318_FAIL_MIGRATION:-0}"
    entrypoint: ["sh", "-ec", "if [ \"$${ISSUE318_FAIL_MIGRATION:-0}\" = \"1\" ]; then echo \"controlled migration failure: assessment-migrations\" >&2; exit 41; fi; exec /workspace/migrate-service.sh --schema assessment"]
    restart: "no"
  grade-migrations:
    image: "onlinejudge/platform-migration-runner:c66686ff0e011f5ee63e3908683f01afd4f83ebc"
    build:
      context: "/private/tmp/oj307-three-c666"
      dockerfile: "deploy/platform/migration-runner.Dockerfile"
      args:
        GIT_SHA: "c66686ff0e011f5ee63e3908683f01afd4f83ebc"
    depends_on:
      mysql-runtime-account-init:
        condition: service_completed_successfully
      assessment-migrations:
        condition: service_completed_successfully
    environment:
      MYSQL_HOST: "mysql"
      MYSQL_PORT: "3306"
      MIGRATION_DATABASE_NAME: "oj_grade"
      MIGRATION_DATABASE_USER: "root"
      MIGRATION_DATABASE_PASSWORD: "${MYSQL_ROOT_PASSWORD:?MYSQL_ROOT_PASSWORD is required}"
      MIGRATION_ROOT: "/workspace/migrations"
      ISSUE318_FAIL_MIGRATION: "${ISSUE318_FAIL_MIGRATION:-0}"
    entrypoint: ["sh", "-ec", "if [ \"$${ISSUE318_FAIL_MIGRATION:-0}\" = \"1\" ]; then echo \"controlled migration failure: grade-migrations\" >&2; exit 41; fi; exec /workspace/migrate-service.sh --schema grade"]
    restart: "no"
volumes:
  assessment-files:
  mysql-data:
