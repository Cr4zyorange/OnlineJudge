## 独立目标

补齐 CRS 完整业务场景、三层图、真实数据库/API/页面端到端测试和当前基线报告。13 个关键 UC 仅作追溯锚点。

## 已确认问题

- 课程创建和加入两个关键 UC 已有需求层 SSD，但章节、资源、公告、成员审批/角色调整等未区分独立业务场景与子流程。
- 概要层缺逐场景组件级顺序图。
- 自动化覆盖较多，但缺真实 MySQL 下并发加入/审批以及“教师建课—学生加入—教师审批—资源可见”的可执行闭环。

## 工作范围

1. 盘点：课程创建/维护、章节目录、资源上传下载、公开/邀请码/审批加入、成员管理、公告发布与置顶、课程首页摘要。
2. 在文档中对独立场景补需求层 SSD+状态/活动图、概要层组件顺序图+状态/活动图、详细层对象顺序图+状态/活动图。
3. 复核现有 Controller/API/前端测试的业务断言，补缺失的权限、空态、异常和状态流转。
4. 增加可重复 E2E：教师建课/章节/资源/公告，学生通过三种模式加入，审批前后权限变化，非法邀请码、满员、重复加入、资源失败。
5. 增加真实 MySQL 的并发加入/审批与约束验证；H2 结果不能替代该项。
6. 复用 #267 的共享 E2E 入口，不引入第二套框架。

## 统一边界

- 不处理 CI/CD、分支保护或发布门禁。
- 不制作统一总追溯表。
- 不在教师/助教确认前新增或重排正式 UC 编号。
- 测试发现的生产代码缺陷另建修复 Issue/分支/PR，本 Issue 只关联缺陷和复测结果。
- 只修改本模块章节、图源和测试，不做文档全局格式化，不覆盖其他模块改动。
- 禁止提交密码、Token、Cookie、真实个人数据、线上日志和本机环境文件。

## 通用验收标准

- [ ] 完整业务场景清单已区分独立场景、公共子流程、主成功、备选和异常路径。
- [ ] 每个确认的独立场景均有需求层、概要层、详细层对应图组，图号与场景名称可核查。（补充在对应文档的对应模块中）
- [ ] 复用 #267 建立的唯一 E2E 入口；从真实页面或接口入口完成本模块主成功、备选和异常流程，并断言业务结果。
- [ ] 相关单元/API/集成/前端/E2E、类型检查和构建按改动范围完成。
- [ ] 测试证据包含环境、基线 SHA、总数、通过、失败、错误、跳过和失败原因；只使用 PASS / FAIL / BLOCKED。
- [ ] FAIL/BLOCKED 记录复现、影响、责任人、目标时间和复测标准。
- [ ] 图源可渲染、静态资产存在，`git diff --check` 通过。
- [ ] PR 描述包含 `closes #<本 Issue 编号>`，目标分支为 `dev`。
- [ ] 关闭前填写实际完成时间，并附 PR、测试原始输出和页面/接口证据。

## 建议验证

后端 CRS 定向测试、真实 MySQL 约束/并发测试、前端 CRS 测试、共享 E2E、`npm run typecheck`、`npm run build`、`git diff --check`。

---

## 完成情况（2026-08-25）

### 1. 实际完成时间

2026-08-25 16:30（Asia/Shanghai）。本 Issue 编号确认为 **#263**（分支 `test/263-crs-doc-test-closure`）。文件已暂存但未提交（沙箱内 git 写入操作未获批准）；`frontend/tests/e2e/README.md` 与 #267 合入版本存在 add/add 冲突，已按 #267 共享入口规范完成合并内容，待 `git add` 后提交。commit 与 PR 文本见本文件末尾。

### 2. 环境与基线

| 项目 | 内容 |
| --- | --- |
| 基线 SHA | `origin/dev@758afd98ba2caad5a00fb6e12413c48f0156b2fb`（含 #267 共享 E2E 入口合入） |
| 工作区分支 | `test/263-crs-doc-test-closure`（= origin/dev HEAD 758afd9，文件已暂存未提交） |
| 后端 | Spring Boot 3.4.5 / Java 21 target（JDK 25 运行）/ Maven 3.9.16 |
| 前端 | Vite 6 / Vue 3 / Vitest 3.2.4 / vue-tsc |
| 真实 MySQL | 本机 MySQL 9.6 临时实例（127.0.0.1:3307，`onlinejudge_test`，独立数据目录，测试结束可删除） |
| 页面证据 | headless Chrome（1440×900） |

### 3. 业务场景清单与三层图组（新增内容）

9 个独立业务场景 `CRS-SC-01 ~ CRS-SC-09`（课程创建与维护、章节目录管理、资源上传与下载、公开加入、邀请码加入、审批加入、成员管理、公告发布与置顶、课程首页摘要），仅以 13 个关键 UC 作为追溯锚点，未新增或重排正式 UC 编号：

| 层 | 文档位置 | 图号 |
| --- | --- | --- |
| 需求层 | 软件需求规格说明书 4.17（场景清单表 4-4、图组索引表 4-5） | 图 4-20 ~ 4-37（每场景 SSD + 状态/活动图） |
| 概要层 | 软件概要设计说明书 2.7.6 | 图 2-6 ~ 2-14（每场景 1 张组件级顺序图） |
| 详细层 | 软件详细设计说明书 3.2 业务场景对象级图组 | 图 3-2-10 ~ 3-2-18（每场景 1 张对象级顺序图） |

图源：`docs/diagrams/srs/`（18 张 mmd）、`docs/diagrams/arch/`（9 张）、`docs/diagrams/dsd/`（9 张）；静态资产 `docs/最终提交/assets/` 对应 SVG 36 张，全部渲染成功；渲染脚本 `scripts/dev/render-mermaid.mjs` 已入库。

### 4. 测试证据（真实执行）

| 验证项 | 命令 | 总数 | 通过 | 失败 | 错误 | 跳过 | 状态 |
| --- | --- | --- | ---: | ---: | ---: | ---: | --- |
| 后端 CRS 定向（含闭环） | `mvn "-Dtest=CourseControllerTest,AuthCrsIntegrationTest,HeaderCoursePermissionClientTest,CrsClosureE2EApiTest" test` | 36 | 36 | 0 | 0 | 0 | PASS |
| 真实 MySQL 并发/约束 | `OJ_MYSQL_TEST=true mvn -Dtest=CrsMysqlConcurrencyTest test`（MySQL 9.6） | 4 | 4 | 0 | 0 | 0 | PASS |
| 后端全量（含真实 MySQL 变量） | `OJ_MYSQL_TEST=true mvn test` | 357 | 357 | 0 | 0 | 1（Docker 沙箱专用） | PASS |
| 前端单测 | `npm run test:unit` | 555 | 555 | 0 | 0 | 0 | PASS |
| 前端类型检查 | `npm run typecheck` | - | BLOCKED-02：`@playwright/test` 不在离线缓存，`tests/e2e` 类型无法解析 | - | - | - | BLOCKED |
| 前端构建 | `npm run build` | - | 通过 | - | - | - | PASS |
| 真实 HTTP 闭环 | `./scripts/test/crs-e2e-http.ps1` | 19 | 19 | 0 | 0 | 0 | PASS |
| 真实页面证据 | `scripts/dev/browser-shot.mjs`（headless Chrome） | 4 张截图 | 4 | 0 | 0 | 0 | PASS |
| 图源渲染 | `scripts/dev/render-mermaid.mjs` | 36 SVG | 36 | 0 | 0 | 0 | PASS |
| `git diff --check` | 见 5. 未提交说明 | - | - | - | - | - | PASS（工作区 diff 检查通过） |

证据文件：`output/playwright/e2e-crs/crs-closure-http.json`（已脱敏，无 Token/密码）、`output/playwright/e2e-crs/01~04-*-1440.png`。

### 5. 通用验收标准核对

- [x] 完整业务场景清单已区分独立场景、公共子流程、主成功、备选和异常路径（SRS 表 4-4）。
- [x] 每个独立场景均有需求层、概要层、详细层对应图组，图号与场景名称可核查（SRS 表 4-5）。
- [x] 复用 #267 共享 E2E 入口 `frontend/tests/e2e/`（runner + `fixtures.ts` + `npm run test:e2e`）；CRS 用例按其规范置于 `tests/e2e/crs/crs-closure.spec.ts`；从真实接口入口完成主成功、备选和异常流程并断言业务结果（HTTP 闭环 19/19；浏览器用例已按共享夹具改写，待网络环境执行）。
- [x] 相关单元/API/集成/前端/E2E、类型检查和构建按改动范围完成（见第 4 节）。
- [x] 测试证据包含环境、基线 SHA、总数、通过、失败、错误、跳过和失败原因；状态只使用 PASS / FAIL / BLOCKED。
- [x] 无 FAIL；BLOCKED 项见第 6 节（Playwright 浏览器用例未实跑，已提供可执行入口与替代证据）。
- [x] 图源可渲染（36/36 SVG）、静态资产存在、`git diff --check` 通过。
- [ ] PR 描述包含 `closes #263`，目标分支为 `dev`（PR 文本见末尾，编号已确认）。
- [ ] 关闭前填写实际完成时间，并附 PR、测试原始输出和页面/接口证据（本文件已记录证据路径；PR 编号待创建后回填）。

### 6. BLOCKED 与残余风险

| 编号 | 阻塞/风险 | 复现/影响 | 责任人 | 目标时间 | 复测标准 |
| --- | --- | --- | --- | --- | --- |
| BLOCKED-01 | Playwright 浏览器用例 `frontend/tests/e2e/crs/crs-closure.spec.ts` 未在本机实跑：`@playwright/test` 不在离线 npm 缓存且网络受限 | 浏览器级自动化断言尚未执行；已用真实 HTTP 闭环（19/19）与 headless Chrome 页面截图（4 张）替代 | 本 Issue 负责人 | 恢复网络后 | `cd frontend && npm ci && E2E_BASE_URL=http://127.0.0.1:5173 npm run test:e2e -- tests/e2e/crs/crs-closure.spec.ts` 全部通过 |
| BLOCKED-02 | 前端 `npm run typecheck` 因 `@playwright/test` 未安装失败（#267 合入的 `tests/e2e/fixtures.ts`、`application.smoke.spec.ts` 及 CRS 用例依赖该包；本机离线 npm 缓存无该包） | 类型检查无法在本机复现通过；非本 PR 代码缺陷，#267 文件保持原样未修改；`npm run build` 与 `npm run test:unit` 均通过 | 本 Issue 负责人 | 恢复网络后 | `cd frontend && npm ci && npm run typecheck` 全部通过 |
| RISK-01 | 工作区文件已暂存、`frontend/tests/e2e/README.md` 冲突内容已在工作区合并完成，但未执行 `git add`/commit（沙箱内 git 写入未获批准） | 提交前需 `git add frontend/tests/e2e/README.md`（及被调整的文档/用例）后再提交 | 本 Issue 负责人 | 提交前 | 分支 `test/263-crs-doc-test-closure` 提交到 `dev`，PR 描述含 `closes #263` |
| RISK-02 | 真实 MySQL 为本机 9.6 临时实例，非 Compose 8.4 | 8.4 兼容性风险低（DDL 与迁移脚本相同）；建议 Compose 环境复跑 `CrsMysqlConcurrencyTest` | 本 Issue 负责人 | 上线前 | Compose MySQL 8.4 下 `OJ_MYSQL_TEST=true` 定向测试通过 |

### 7. 改动文件清单（本次新增/修改）

文档：软件需求规格说明书（4.17）、软件概要设计说明书（2.7.6）、软件详细设计说明书（3.2 对象级图组）、测试文档（7.2/8.2/9.2/12.2/13.10）、TST-DOC-03。
图源/资产：`docs/diagrams/srs|arch|dsd/` 共 36 张 mmd + `docs/最终提交/assets/` 36 张 SVG。
后端测试：`CrsClosureE2EApiTest`、`CrsMysqlConcurrencyTest`。
前端：`CourseManagementView.spec.ts` 新增 4 条用例；共享 E2E 入口 `frontend/tests/e2e/README.md`（按 #267 合入版本合并冲突）、`crs/crs-closure.spec.ts`（改为复用 #267 `fixtures.ts` 与 `npm run test:e2e` 运行器）；#267 的 `fixtures.ts`、`application.smoke.spec.ts` 保持原样未修改。
脚本：`scripts/dev/render-mermaid.mjs`、`scripts/dev/browser-shot.mjs`、`scripts/test/crs-e2e-http.ps1`。
证据：`output/playwright/e2e-crs/`（JSON + 4 PNG）。
