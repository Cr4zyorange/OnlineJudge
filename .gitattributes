*.sh text eol=lf

# Preserve Docker/Compose console captures as test evidence with a repository-stable
# line ending so their documented SHA-256 values are directly reproducible.
output/issue-265/*.log text eol=lf -whitespace
