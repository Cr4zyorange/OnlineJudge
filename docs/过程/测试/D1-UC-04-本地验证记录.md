# D1-UC-04 本地验证记录

## 范围

- Issue：`#245 [D1-UC-04] 验证 UC-LAB-01～02、UC-GR-04`
- 需求依据：`docs/最终提交/软件需求规格说明书.md` 第 4.6、4.9、4.13 节
- 基线：`3a802574415658df98a5df787a31f2c7590897f7`（`dev`）
- 本地分支：`test/245-uc-lab-grade-validation`
- 关联修复：`#250`，修复 PR `#252`，本地提交 `7a35527`

本记录先固定本地验证结果；只有在验证结论、测试统计和缺陷复测齐全后，才能更新远端 Issue、推送分支或创建 PR。

## 可重复执行方式

先在隔离的临时数据库启动后端。以下环境变量防止本次验证写入仓库内的 `backend/data/`：

```powershell
$env:SERVER_PORT = '18080'
$env:SPRING_DATASOURCE_URL = 'jdbc:h2:mem:issue245;MODE=MySQL;DATABASE_TO_LOWER=TRUE;CASE_INSENSITIVE_IDENTIFIERS=TRUE;DB_CLOSE_DELAY=-1'
$env:ONLINEJUDGE_STORAGE_LOCAL_ROOT = "$env:TEMP\onlinejudge-issue245-uploads"
Set-Location backend
mvn spring-boot:run
```

在另一终端执行：

```powershell
./scripts/test/verify-issue-245.ps1
```

脚本通过项目登录接口建立教师和学生会话，不启用 `onlinejudge.auth.allow-header-auth`。它验证的行为包括：

- UC-LAB-01：教师创建草稿、学生不可见草稿、教师发布、过去截止时间和学生创建权限被拒绝。
- UC-LAB-02：两次代码提交的版本递增、非法报告附件被拒绝、两次报告上传的版本递增、历史倒序和 Docker 不可用时 `SYSTEM_ERROR` 可查询。
- UC-GR-04：学生提交、重复待处理申请被拒绝、学生不得处理、教师驳回、重新提交、教师同意调整，以及成绩总评重算。

## 自动化回归

```powershell
Set-Location backend
mvn test "-Dtest=LabExperimentControllerTest,LabSubmissionControllerTest,LabEvaluationServiceTest,GradeReviewServiceTest"
```

执行日期：2026-08-25。

| 测试类 | 用例数 | 结果 |
| --- | ---: | --- |
| `LabExperimentControllerTest` | 8 | PASS |
| `LabSubmissionControllerTest` | 36 | PASS |
| `LabEvaluationServiceTest` | 1 | PASS |
| `GradeReviewServiceTest` | 3 | PASS |
| 合计 | 48 | PASS，0 failures / 0 errors / 0 skipped |

`LabEvaluationServiceTest` 会按设计记录一次 `sandbox offline` 异常日志，用于断言评测异常会持久化为可追踪的系统错误；该测试本身通过。

关联修复在 `fix/250-docker-sandbox-status` 分支补充了 `DockerSandboxExecutorTest` 回归：5 条测试中 4 条通过、1 条因本机 Docker daemon 未启动而按环境假设跳过；伪 Docker 命令测试稳定覆盖“编译阶段 daemon 不可用 -> SYSTEM_ERROR”“运行阶段 daemon 不可用 -> SYSTEM_ERROR”和“普通语法错误 -> COMPILE_ERROR”。完整后端回归结果为 344 条测试，`0 failures / 0 errors / 1 skipped`。

前端定向回归命令：

```powershell
Set-Location frontend
npm run test:unit -- tests/unit/lab tests/unit/grd tests/unit/api/labs.spec.ts src/views/grd/StudentGradeView.spec.ts
```

执行日期：2026-08-25；结果：20 个测试文件、226 条测试均通过。

## 运行结果

| 用例 | 主流程 | 异常/权限分支 | 结果 | 证据 |
| --- | --- | --- | --- | --- |
| UC-LAB-01 | 教师创建草稿后发布为 `PUBLISHED` | 学生创建返回 403；过去截止时间返回 400；学生读取草稿返回 403 | PASS | API 验收脚本于 2026-08-25 10:53 执行；相关断言通过 |
| UC-LAB-02 | 代码两次提交版本由 1 递增到 2；报告两次上传版本由 1 递增到 2；历史倒序可读 | 非法报告附件返回 400；Docker daemon 不可用时自动评测结果为 `SYSTEM_ERROR`，可读取失败信息 | PASS | API 验收脚本于 2026-08-25 10:53 执行；关联修复为 `#250` / PR `#252` / `7a35527` |
| UC-GR-04 | 学生提交异议，教师驳回后可重新提交，教师同意调整后总评从 89.60 重算为 90.80 | 重复 `PENDING` 返回 400 / `ERR-GRD-08`；学生处理返回 403 / `ERR-AUTH-05` | PASS | API 验收脚本于 2026-08-25 10:53 执行；相关断言通过 |

本次最终 API 验收使用临时 H2 数据库 `issue245fix`、端口 `18081` 和临时上传目录 `%TEMP%\\onlinejudge-issue245-uploads-fix`。之所以改用 `18081`，是因为本机已有其他进程占用 `18080`。Docker CLI 可用但 Docker daemon 未启动；该运行条件用于验证沙箱基础设施故障不会被错误归类为学生代码编译失败。

## 本地缺陷记录

| 编号 | 严重程度 | 状态 | 现象 | 预期 | 定位与复测条件 |
| --- | --- | --- | --- | --- | --- |
| `LOCAL-245-01` | P1 | 已修复，待 PR 评审 | Docker daemon 不可用时，实验自动评测 API 曾返回 `COMPILE_ERROR`。 | 沙箱基础设施不可用应标记为 `SYSTEM_ERROR`，并保留失败信息；不能把平台故障描述为学生代码编译错误。 | 独立 Issue `#250` 的 `fix/250-docker-sandbox-status` 分支在 `DockerSandboxExecutor` 中识别 daemon 连接失败信息；修复 PR 为 `#252`，其 RED/GREEN 测试和完整后端回归通过后，本脚本最终复测为 PASS。 |

该缺陷未混入 #245 的验证变更；修复代码、测试和 PR 均归属独立 Issue `#250`。#245 的 PR 依赖修复 PR `#252` 先合并到 `dev`，否则验收结果无法在目标分支持续复现。

执行脚本后，只能将结果写为 `PASS`、`FAIL` 或 `BLOCKED`，并记录实际时间、HTTP 状态、错误码和日志路径。若发现产品缺陷，应建立独立修复 Issue；本验证 Issue 不混入修复代码。
