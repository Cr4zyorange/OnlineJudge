# 用户权限与平台安全模块

## 一、负责人

- 横向角色：需求负责人
- 负责模块：用户权限与平台安全
- 需求编号：`FR-UA`（功能）、`NFR-UA`（非功能）
- 测试前缀：`TC-UA`

## 二、章节审查

- `2.4` 运行环境：无问题
- `2.5` 技术与运行约束：无问题
- `2.6` 模块划分：无问题
- `3` 用户角色分析：建议明确学生、教师、管理员三类角色的权限边界
- `4` 核心业务流程与用例：建议补充“用户登录并按角色访问平台功能”用例
- `7` 数据需求：建议补充用户表、角色表、权限表、用户角色关联表、操作日志表等数据需求

## 三、用户权限与平台安全

### 3.1 功能目标

本模块负责平台用户身份认证、角色权限控制、访问控制与基础安全防护，保障不同角色在系统中的功能边界清晰、数据访问受控、关键操作可追踪，从而确保教学平台运行安全、数据安全与业务合规。

### 3.2 需求项

#### FR-UA-01 用户注册与登录（P 0）

1. 用户应能够通过学号/工号、手机号、邮箱或系统指定账号完成注册与登录。
2. 系统应支持学生、教师、管理员等不同类型用户身份的识别。
3. 用户登录成功后，系统应创建有效会话，并允许用户在授权范围内访问对应功能。
4. 用户退出登录后，系统应及时清除当前会话或令牌，使其不能继续访问需认证资源。

#### FR-UA-02 角色管理与权限分配（P 0）

1. 系统应支持至少包含学生、教师、管理员三类角色。
2. 不同角色应具有不同的功能访问范围：学生侧重学习与提交，教师侧重课程与教学管理，管理员侧重平台级维护。
3. 管理员应能够对用户角色进行查看、分配、调整与维护。
4. 系统应确保未被授权的用户不能访问超出其角色范围的功能菜单、接口与数据。

#### FR-UA-03 身份认证与访问控制（P 0）

1. 用户访问课程管理、作业管理、成绩管理、实验管理等核心业务功能前应先完成身份认证。
2. 系统应对页面访问、接口调用和资源下载进行权限校验。
3. 学生只能访问本人已加入课程、本人作业、本人实验记录和本人成绩等数据。
4. 教师只能访问本人负责课程范围内的教学管理数据。
5. 管理员可在平台授权范围内访问全局管理数据并执行维护操作。

#### FR-UA-04 账号信息与密码安全（P 0）

1. 用户应能够修改个人基础信息，如昵称、联系方式、头像等。
2. 用户应能够修改登录密码，并在修改时校验原密码或采用其他有效身份验证方式。
3. 系统不得以明文形式存储用户密码。
4. 当用户连续多次登录失败时，系统应进行限制或提醒，以降低暴力破解风险。

#### FR-UA-05 权限异常处理与安全提示（P 1）

1. 当用户访问无权限页面、接口或资源时，系统应明确提示“无权限访问”或返回相应失败结果。
2. 当用户登录状态失效、会话过期或认证信息无效时，系统应提示重新登录。
3. 对于被禁用、被冻结或异常状态的账号，系统应限制其登录和业务访问。
4. 系统在发生关键安全异常时，应保留必要日志以便排查。

#### FR-UA-06 关键操作审计（P 1）

1. 系统应记录用户登录、退出登录、角色变更、密码修改、权限调整等关键操作。
2. 审计日志至少应包含操作时间、操作用户、操作类型、操作对象和结果状态。
3. 管理员应能够查看必要的安全审计记录，用于问题追踪与责任定位。

#### FR-UA-07 平台基础安全防护（P 1）

1. 系统应对登录、注册、密码修改等输入内容进行合法性校验。
2. 系统应对非法请求、缺失认证信息请求和越权请求进行拦截。
3. 系统应避免因参数篡改导致越权访问、越权修改或越权删除。
4. 系统应在文件上传、资源访问、接口调用等场景中进行必要的安全校验。

### 3.3 异常流程补充

- 用户名或密码错误
- 账号不存在
- 账号已被禁用或冻结
- 登录状态失效
- 用户无权限访问目标页面或资源
- 用户尝试访问他人课程、作业、成绩或实验数据
- 用户连续多次登录失败
- 权限分配失败或角色配置异常
- 关键安全日志记录失败

### 3.4 验收标准

- Given 学生已成功登录，When 学生访问本人已加入课程的学习资源，Then 系统应允许访问。
- Given 学生已成功登录，When 学生访问教师课程管理页面或管理员后台，Then 系统应拒绝访问并提示无权限。
- Given 教师已成功登录，When 教师进入本人负责课程的管理页面，Then 系统应允许其进行课程、作业、实验等教学管理操作。
- Given 管理员已成功登录，When 管理员调整某用户角色权限，Then 系统应完成修改并记录审计日志。
- Given 用户登录状态已过期，When 用户继续访问需认证页面或接口，Then 系统应提示重新登录。
- Given 用户连续多次输入错误密码，When 超过系统限制次数，Then 系统应执行限制策略或发出安全提示。

## 四、核心业务流程与用例补充

### 用例：用户登录并按角色访问平台功能

#### 1. 用例名称

用户登录并按角色访问平台功能

#### 2. 参与角色

- 学生
- 教师
- 管理员

#### 3. 前置条件

- 用户已拥有有效账号。
- 用户账号状态正常，未被冻结或禁用。
- 平台认证与权限服务运行正常。

#### 4. 基本流程

1. 用户进入平台登录页面。
2. 用户输入账号和密码并提交登录请求。
3. 系统校验用户身份信息。
4. 身份校验通过后，系统创建登录会话或访问令牌。
5. 系统根据用户角色加载对应的功能菜单和操作入口。
6. 用户访问其权限范围内的课程、作业、实验、成绩或管理功能。
7. 系统在用户访问页面、接口或资源时进行权限校验。
8. 权限校验通过后，系统返回对应页面或数据。

#### 5. 备选/异常流程

- 用户输入错误账号或密码，系统提示登录失败。
- 用户账号不存在，系统提示账号或密码错误。
- 用户账号被冻结或禁用，系统拒绝登录。
- 用户登录状态失效，系统要求重新登录。
- 用户访问无权限功能，系统拒绝访问并提示无权限。
- 用户通过修改 URL 或请求参数访问他人数据，系统拦截请求并返回无权限结果。
- 系统记录关键认证失败、越权访问或权限变更日志。

#### 6. 后置条件

- 用户成功进入其角色对应的功能范围。
- 用户仅能访问被授权的数据和功能。
- 未授权访问被系统拒绝。
- 关键安全操作被记录到审计日志中。

## 五、用户权限与平台安全

### NFR-UA-01 安全性

- 系统必须对所有需认证功能进行身份校验与权限校验。
- 用户密码不得以明文形式存储、展示或传输。
- 系统应防止未授权访问、越权访问和通过简单参数篡改造成的数据泄露。
- 系统应对登录、注册、密码修改、文件上传、资源访问等关键场景进行必要的安全校验。

### NFR-UA-02 可靠性

- 登录、退出、密码修改、角色调整等关键安全功能应稳定可用。
- 权限校验失败时，系统应返回明确结果，不得出现“界面限制但接口可绕过”的情况。
- 审计日志在正常情况下应完整记录关键安全事件。
- 当认证服务异常时，系统应避免默认放行未授权请求。

### NFR-UA-03 可用性

- 登录、退出登录、修改密码等操作流程应清晰易用。
- 权限不足、登录失效、账号异常等情况应提供明确提示信息。
- 不同角色登录后应看到与自身身份匹配的功能入口，避免展示无关或不可用功能。

### NFR-UA-04 性能

- 在课程平台常规使用场景下，登录认证、权限校验和常见鉴权接口应尽量在 3 秒内完成响应。
- 权限判断不应对普通页面访问和常规教学操作造成明显卡顿。
- 系统应避免因频繁权限查询导致页面加载明显变慢。

### NFR-UA-05 可测试性

- 需能够验证学生、教师、管理员三类角色的访问边界。
- 需能够验证登录成功、登录失败、会话失效、越权访问、密码修改、角色调整、审计日志记录等关键场景。
- 需能够通过测试用例确认页面权限与接口权限保持一致。

## 六、需求追踪性

| 需求模块 | 需求编号范围 | 主要用例 | 后续设计关注点 | 建议测试编号前缀 |
| --- | --- | --- | --- | --- |
| 用户权限与平台安全 | `FR-UA-01` - `FR-UA-07`，`NFR-UA-01` - `NFR-UA-05` | 用户登录并按角色访问平台功能 | 认证机制、角色权限模型、接口鉴权、会话管理、密码安全、审计日志 | `TC-UA` |
| 课程与教学资源 | `FR-CR-01` - `FR-CR-06`，`NFR-CR-01` - `NFR-CR-05` | 学生加入课程并开始学习 | 教师课程管理、学生加入课程审核、资源上传、课程资源访问权限设置 | `TC-CR` |
| 学习过程与通知提醒 |  |  |  | `TC-LN` |
| 实训实验模块 |  |  |  | `TC-LAB` |
| 作业与自动评测模块 | `FR-HWK-01` - `FR-HWK-06`，`NFR-HWK-01` - `NFR-HWK-05` | 学生提交作业、自动评测 | 作业模型、评测状态机、重评机制 | `TC-HWK` |
| 成绩评价与教学分析 |  |  |  | `TC-GR` |

## 七、D2 业务场景清单补充

按 Issue #261 盘点，AUTH 模块的完整业务场景分类如下（正式 UC 编号以《软件需求规格说明书》为准，不新增、不重排）：

| 场景 | 名称 | 分类 | 需求锚点 |
| --- | --- | --- | --- |
| S1 `UC-AUTH-01-R` | 注册、登录、退出与持续鉴权 | 独立业务场景 | FR-UA-01、FR-UA-03、FR-UA-05 |
| S2 `UC-AUTH-01-P` | 个人资料与密码安全 | 独立业务场景 | FR-UA-04、FR-UA-07 |
| S3 `UC-AUTH-01-A` | 管理员用户、角色与权限管理 | 独立业务场景 | FR-UA-02、FR-UA-03、FR-UA-06 |
| S4 `UC-AUTH-01-S` | 安全异常处理与关键操作审计 | 独立业务场景 | FR-UA-05、FR-UA-06、FR-UA-07 |
| C1 ~ C5 | 账号密码校验、会话签发与校验、角色菜单加载、审计写入与查询、统一认证与权限拦截 | `include` 公共子流程 | 由 S1 ~ S4 复用 |
| E1 ~ E7 | 错误凭据、禁用/冻结、失败锁定、令牌失效、越权访问、输入校验失败、审计写入失败 | 备选/异常路径 | 见《软件需求规格说明书》异常流程与 DSD ERR-AUTH-* |

场景清单、三层图组映射与测试证据详见 `docs/过程/测试/TST-DOC-02-AUTH-业务场景清单与测试闭环.md`。
