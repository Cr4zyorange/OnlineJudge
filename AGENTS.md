# AGENTS.md

本文件是本仓库内所有 AI 编码代理的强制协作规范。凡是在本仓库中编写、修改、评审或提交代码，必须先阅读并遵守本文件；若本文件与临时习惯冲突，以本文件和仓库内已提交的设计文档为准。

## 当前阶段

本项目已经结束设计阶段，正式进入代码开发阶段。开发目标不是各模块各写各的，而是按设计文档把系统主流程真正跑通：

```text
登录 -> 课程 -> 作业/实验 -> 提交/评测 -> 成绩 -> 通知
```

所有实现必须服务于这个闭环。不能只完成孤立的页面、孤立的接口或孤立的数据表。

## 工作方法：文档驱动 + 测试驱动

本仓库开发采用双驱动：

```text
文档驱动决定做什么、边界在哪里、接口和数据结构是什么；
测试驱动决定怎么证明它真的工作。
```

文档驱动已经由 `docs/开发/`、需求规格说明书、概要设计说明书、详细设计说明书共同约束。AI 代理不能凭感觉补需求、改接口或改模块边界。

测试驱动是代码实现的进入条件。除纯文档、纯配置、一次性脚手架外，任何新功能、缺陷修复、重构和行为调整都必须先定义可执行验证，再写实现代码。

## 开发前必须阅读

开始任何代码修改前，必须按当前 issue 或模块阅读以下材料：

1. `docs/开发/` 中对应模块的开发流程文档。
2. `docs/最终提交/软件需求规格说明书.md`。
3. `docs/最终提交/软件概要设计说明书.md`。
4. `docs/最终提交/软件详细设计说明书.md`。
5. 对应模块在 `docs/过程/需求/`、`docs/过程/概要/`、`docs/过程/详细设计/` 下的源文档。
6. 涉及前端页面、组件、布局或样式时，还必须阅读 `docs/开发/` 中对应模块的“前端页面与交互”章节，以及仓库内 UI 设计规范或 UI 参考材料；当前 UI 参考位于 `docs/过程/UI设计参考/`。

`docs/开发/` 给出推荐开发顺序；最终提交目录中的三份设计说明书是需求、接口、数据结构、模块边界和验收行为的主要依据。若二者不一致，先以最终提交文档为准，再检查过程文档和现有代码，不能自行静默改设计。

## 页面与 UI 设计规范

实现任何前端页面时，必须按仓库文档中的 UI 设计规范和参考稿落地，不允许只按个人审美临时发挥。页面开发至少遵守：

- 先对照 `docs/最终提交/软件详细设计说明书.md` 第 4 章界面设计，确认对应 `UI-*` 编号、页面职责和角色入口。
- 再对照 `docs/开发/` 中对应模块的“前端页面与交互”章节，确认页面清单、完成标准、加载/成功/失败/空状态和权限状态。
- 视觉风格、布局结构、导航、侧边栏、毛玻璃卡片、背景图、色彩和交互基调，应优先沿用仓库 UI 设计规范或 `docs/过程/UI设计参考/` 中的 `index.html`、`style.css`、`img/back.jpg`。
- 新增页面必须接入真实 API 或已在 issue/PR 中明确标注的稳定联调契约；不能提交只展示静态假数据、与 UI 参考风格割裂、或无法进入主流程的孤立页面。
- 前端样式调整后必须运行类型检查、构建、组件测试或浏览器验收；若只做纯样式微调，也至少记录页面构建或浏览器检查范围。

## 模块开发顺序

每个模块都必须按完整纵向链路推进，不允许跳着做：

```text
数据库/迁移/种子数据
-> 后端 API
-> Service 业务逻辑
-> 前端页面和 API 调用
-> 登录态、权限校验和课程成员校验
-> 成功、失败、空状态、异常状态提示
-> 测试和本地自测
-> 跨模块联调
```

涉及六个模块时，优先级遵守：

1. `AUTH`：登录、当前用户、角色、权限、测试账号。
2. `CRS`：课程、章节、资源、课程成员和课程权限。
3. `LAB` / `HWK`：实验、作业、提交、评测和共享评测状态。
4. `GRD`：消费 LAB/HWK 来源成绩，完成成绩计算、发布和查询。
5. `LRN`：通知、提醒、学习过程记录和跨模块事件消费。

## 测试驱动规则

功能代码必须遵守 Red-Green-Refactor：

```text
RED：先写一个失败的测试或验收用例，证明当前行为缺失或错误。
GREEN：只写足够通过该测试的最小实现。
REFACTOR：在测试保持通过的前提下清理结构、命名和重复代码。
```

硬性规则：

- 没有失败测试，不写生产代码。
- 没有亲眼确认测试失败，不认为测试有效。
- 测试通过后才能重构；重构后必须重新运行相关测试。
- 每个 issue 至少要有一个能证明核心行为的自动化测试或可重复执行的验收脚本。
- 测试名称必须描述业务行为，不能只写 `test1`、`should work` 这类空泛名称。
- 优先测试真实业务代码；只有外部服务、时间、文件系统、网络等不可控依赖才使用 mock。
- 不能为了让测试通过而降低断言、删除关键覆盖或把真实错误吞掉。

允许的例外必须写明原因：

- 纯文档改动：运行 `git diff --check`。
- 纯样式微调：至少做页面构建或浏览器手工验收，并记录检查范围。
- 项目尚未建立测试框架：必须先补最小测试入口，或在 PR 中写明当前阻塞和替代验证。

## 测试覆盖边界

测试必须跟随模块纵向链路，而不是只测某一层：

- 后端 Service：覆盖核心业务规则、权限分支、异常分支和状态流转。
- 后端 Controller/API：覆盖请求字段、响应格式、错误码、分页和鉴权失败。
- 数据库迁移：覆盖表结构可创建、关键约束生效、种子数据可用。
- 前端组件/页面：覆盖表单校验、加载态、空状态、失败提示、权限不可见或不可操作。
- 跨模块集成：覆盖 AUTH 当前用户、CRS 课程成员、LAB/HWK 来源成绩、GRD 成绩发布、LRN 通知事件等公共契约。

当一个 issue 横跨前后端时，测试也必须横跨前后端：至少要有后端行为测试和前端交互或端到端验证，不能只补一侧。

## 设计边界

严禁随意修改以下公共约定：

- 需求范围和功能边界。
- REST API 路径、请求字段、响应格式、错误码。
- 数据库表结构、字段语义、索引和状态枚举。
- 跨模块 DTO、事件对象和集成客户端。
- 模块职责边界，尤其是 AUTH、CRS、LAB、HWK、GRD、LRN 之间的依赖方向。

如果实现时发现设计不合理，必须先在提交说明、PR 描述或项目沟通记录中明确说明调整原因、影响范围和相关模块。会影响其他模块的改动，不能静默提交。

## Issue 和 PR 规则

开发必须围绕 GitHub Projects 中的 issue 推进：

- 一个 issue 对应一个 PR。
- 开始开发前确认 issue 内容、所属模块和当前 Projects 状态。
- 开始做时将 issue 移动到进行中。
- PR 目标分支统一为远程 `dev`，本仓库的 `dev` 等价于常见 Git Flow 中的 `develop`。
- PR 描述必须包含 `close #issue_id` 或 `Closes #issue_id`。
- 没有关联 issue 的开发改动不应混入功能 PR。

如果某个 issue 做不完、卡住，或发现范围明显大于预期，必须尽早在 issue/PR 中写明当前进展、阻塞点和需要协同的模块。

## 分支和同步规则

本仓库采用接近 Git Flow 的分支模型，但实际开发主干使用 `dev`，不要擅自新建平行的 `develop` 分支。

| 分支 | 用途 | 来源 | 合并去向 | 环境 | 是否可直接提交 |
| --- | --- | --- | --- | --- | --- |
| `main` | 稳定主分支，代表生产可发布版本 | `release/*` 或 `hotfix/*` | 无 | `PRO` | 否 |
| `dev` | 开发集成分支，等价于 `develop` | `main` 或已稳定的 `dev` | `test/*`、`release/*` | `DEV` | 否 |
| `feature/<issue-id>-<name>` | 单个 issue 的功能开发 | 最新 `dev` | PR -> `dev` | 无固定环境 | 是 |
| `fix/<issue-id>-<name>` | 开发期普通 bug 修复 | 最新 `dev` | PR -> `dev` | 无固定环境 | 是 |
| `test/<name>` | 功能验收测试分支，给测试人员或 FAT 环境使用 | `dev` 或指定功能集合 | `release/*` 或回到 `dev` | `FAT` | 限测试修复 |
| `release/<version>` | 预上线分支，用于 UAT、版本冻结和发布前修复 | `test/*` 或稳定 `dev` | `main` 和 `dev` | `UAT` | 限发布修复 |
| `hotfix/<issue-id>-<name>` | 线上紧急修复 | 最新 `main` | `main` 和 `dev` | 无固定环境 | 是 |

环境对应关系：

- `DEV`：开发者自测环境，对应 `dev` 和功能分支。
- `FAT`：功能验收测试环境，对应 `test/*`。
- `UAT`：用户验收测试环境，对应 `release/*`。
- `PRO`：生产环境，对应 `main`。

写代码前必须确认工作区状态并同步目标基线：

```bash
git status --short --branch
git fetch origin
```

功能开发从最新 `dev` 拉分支：

```bash
git switch dev
git pull --ff-only origin dev
git switch -c feature/<issue-id>-<short-name>
```

线上紧急修复从最新 `main` 拉分支：

```bash
git switch main
git pull --ff-only origin main
git switch -c hotfix/<issue-id>-<short-name>
```

分支命名必须遵守：

```text
feature/<issue-id>-<short-name>
fix/<issue-id>-<short-name>
docs/<issue-id>-<short-name>
test/<name>
release/<version>
hotfix/<issue-id>-<short-name>
```

禁止直接在 `dev`、`main`、`release/*` 上写常规功能代码。`release/*` 只允许发布前阻断问题修复；`main` 只接受 `release/*` 或 `hotfix/*` 合并；`hotfix/*` 修复完成后必须同时回合到 `main` 和 `dev`。

在已有未提交改动的工作区中，AI 代理不得覆盖、回滚或格式化与当前任务无关的文件；发现他人改动时必须绕开，必要时只说明风险。

## Commit 规则

提交信息统一使用：

```text
type(scope): message
```

允许的 `type`：

- `feat`：新增功能。
- `fix`：修复 bug。
- `docs`：仅文档变更。
- `style`：不影响代码语义的格式、空白、缺失分号等。
- `refactor`：既不修复 bug 也不新增功能的代码重构。
- `perf`：性能优化。
- `test`：添加、修正或删除测试。
- `chore`：构建过程、辅助工具、依赖、仓库维护等杂项。

单次提交必须遵守：

- 一次 commit 只处理同一类别的问题。
- 一个 commit 不超过 3 个紧密相关的问题。
- 不把功能、修复、格式化、重构、测试、文档混在同一个 commit。
- 提交信息不合规时，优先用 `git commit --amend` 修正尚未推送的最后一次提交。
- 需要拆分或重做提交时，优先使用 `git reset --soft` 或 `git reset --mixed`；禁止随意使用 `git reset --hard`，除非用户明确要求并已确认不会丢失他人改动。
- 提交前必须确认没有临时代码、调试输出、无关文件、密钥、密码或本地环境文件。

## 完成标准

每个 issue 至少满足以下条件才算完成：

- 已先写出失败测试或可执行验收用例，并确认它按预期失败。
- 后端接口能正常调用。
- 前端页面能完成对应操作。
- 成功、失败、空数据、权限不足、会话过期等基本状态有合理提示。
- 涉及权限的地方已经接入登录态和权限校验。
- 涉及课程数据的地方已经接入课程成员和课程权限校验。
- 本地至少跑通过一遍主要业务流程。
- 必要的单元测试、接口测试、集成测试或前端测试已经补齐。
- 相关测试已从失败变为通过，且重构后仍然通过。
- 没有静默修改公共接口、数据库结构、状态枚举或跨模块 DTO。

不能只因为“代码写了”就标记完成。能跑通、能联调、能被评审，才是完成。

## 本地验证

提交前根据改动范围执行相应验证。优先使用仓库已有脚本；如果脚本缺失，再使用各子项目标准命令。

最低要求：

- 后端改动：运行编译、单元测试或对应模块测试。
- 前端改动：运行类型检查、构建、组件测试或端到端流程验证。
- 数据库改动：确认迁移脚本可重复执行，种子数据与实体、DTO 一致。
- 跨模块改动：至少验证一条从前端到后端再到数据库的完整链路。
- 文档或协作规则改动：运行 `git diff --check`。

如果验证无法运行，必须在最终说明或 PR 中写明原因、已做的替代检查和残余风险。

## 公共接口变更

以下变更必须显式同步相关模块，不能作为局部实现细节处理：

- 接口字段、返回格式、分页结构、错误码。
- 数据库表结构、字段含义、约束、状态枚举。
- LAB/HWK 评测状态、评测结果对象、沙箱执行约定。
- GRD 消费的来源成绩 DTO 和成绩发布状态。
- LRN 通知事件类型、事件载荷和触发时机。
- AUTH 当前用户、角色、权限码、登录失效行为。
- CRS 课程成员、课程权限和课程存在性校验。

公共契约调整必须同时更新代码、类型定义、测试和相关文档。

## AI 代理工作方式

AI 代理在本仓库工作时必须：

- 先读现有代码和设计文档，再动手实现。
- 先写失败测试或验收用例，再写实现；不能先实现后补测试。
- 按 issue 做一个完整、自洽、可评审的交付单元。
- 优先沿用仓库既有目录结构、命名、技术栈和测试方式。
- 保持前后端类型、DTO、枚举和错误处理一致。
- 不生成脱离设计文档的临时方案、演示假接口或无法联调的孤立页面。
- 不把可自己完成的下一步交给用户确认；除非继续做会产出与用户意图相反的结果。
- 完成后报告改了什么、为什么这样做、验证了什么、还有什么残余风险。

本仓库的开发纪律是：文档定边界，测试定完成；按设计、按 issue、按分支、按闭环交付。
